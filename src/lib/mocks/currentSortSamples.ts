import { SortType } from '../utils/types/types';

export const currentSortNameAscending: SortType = {
  key: 'name',
  direction: 1,
  type: 'string',
};

export const currentSortNameDescending: SortType = {
  key: 'name',
  direction: -1,
  type: 'string',
};
